name: python

description: Python project template with virtual environment setup

version: 0.1.0

commands:
  - cmd: ["python", "--version"]
  - cmd: ["git", "init"]

files:
  copy:
    - files/README.md
    - files/.gitignore
    - files/main.py

