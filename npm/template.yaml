name: npm

commands:
  - cmd: ["node", "--version"]
  - cmd: ["npm", "--version"]

  - cmd: ["npm", "init"]
    interactive: true
    test_cmd: ["npm", "init", "-y"]

  - cmd: ["git", "init"]

files:
  copy:
    - files/.gitignore
    - files/README.md
    - files/index.js
