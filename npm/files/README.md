# npm Template Files

This template provides a minimal template for starting a Node.js project.

How to use
- Replace `index.js` with your application code or adjust the `start` script.
- The included `.gitignore` covers common Node ignores; the template also appends additional gitignore entries if needed.
